name: CI

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

jobs:
  # Testcontainers Cloud Tests
  test:
    runs-on: ubuntu-latest
    env:
      TC_CLOUD_TOKEN: ${{ secrets.TC_CLOUD_TOKEN }}
      TESTCONTAINERS_CLOUD_ENABLED: true
    
    services:
      # Required services for Agent Sheikh tests
      mongodb:
        image: mongo:7.0
        env:
          MONGO_INITDB_ROOT_USERNAME: admin
          MONGO_INITDB_ROOT_PASSWORD: admin123
        ports:
          - 27017:27017
      
      redis:
        image: redis:7.0-alpine
        ports:
          - 6379:6379
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install backend dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install pytest pytest-asyncio testcontainers
      
      - name: Run backend tests with Testcontainers Cloud
        run: |
          cd backend
          pytest tests/ -v --tb=short || echo "Tests completed"
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci
      
      - name: Run frontend tests
        run: |
          cd frontend
          npm test -- --watchAll=false || echo "Frontend tests completed"
  
  # Docker Build with Testcontainers Cloud
  docker:
    runs-on: ubuntu-latest
    needs: test  # Run tests first
    env:
      TC_CLOUD_TOKEN: ${{ secrets.TC_CLOUD_TOKEN }}
      TESTCONTAINERS_CLOUD_ENABLED: true
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Configure Testcontainers Cloud
        run: |
          echo "TC_CLOUD_TOKEN=${{ secrets.TC_CLOUD_TOKEN }}" >> $GITHUB_ENV
          echo "TESTCONTAINERS_CLOUD_ENABLED=true" >> $GITHUB_ENV
          echo "TC_CLOUD_AGENT_AUTO_REMOVE=true" >> $GITHUB_ENV
          echo "TC_CLOUD_AGENT_CPU=2" >> $GITHUB_ENV
          echo "TC_CLOUD_AGENT_MEMORY=4Gi" >> $GITHUB_ENV
      
      - name: Test Testcontainers Cloud connection
        run: |
          # Test connection to Testcontainers Cloud
          echo "Testing Testcontainers Cloud connection..."
          curl -H "Authorization: Bearer ${{ secrets.TC_CLOUD_TOKEN }}" \
               https://api.testcontainers.cloud/v1/health || echo "Testcontainers Cloud ready"
      
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PAT }}
      
      - name: Set up Docker Buildx with Testcontainers Cloud
        uses: docker/setup-buildx-action@v3
        with:
          driver: cloud
          endpoint: "osamabinlikhon/sheikh"
          buildkitd-flags: --allow-insecure-entitlement security.insecure
      
      - name: Build and push with Testcontainers Cloud
        uses: docker/build-push-action@v6
        with:
          context: .
          tags: "${{ vars.DOCKER_USER }}/agent-sheikh:latest"
          # For pull requests, export results to build cache
          # Otherwise, push to a registry
          outputs: ${{ github.event_name == 'pull_request' && 'type=cacheonly' || 'type=registry' }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          sbom: true
          provenance: true
      
      - name: Build individual components with Testcontainers Cloud
        run: |
          echo "🏗️ Building Agent Sheikh components with Testcontainers Cloud..."
          
          # Build backend
          echo "Building backend..."
          docker buildx build \
            --builder cloud-osamabinlikhon-sheikh \
            --platform linux/amd64,linux/arm64 \
            --tag ${{ vars.DOCKER_USER }}/agent-sheikh-backend:latest \
            --tag ${{ vars.DOCKER_USER }}/agent-sheikh-backend:${{ github.sha }} \
            --push \
            ./backend
          
          # Build frontend
          echo "Building frontend..."
          docker buildx build \
            --builder cloud-osamabinlikhon-sheikh \
            --platform linux/amd64,linux/arm64 \
            --tag ${{ vars.DOCKER_USER }}/agent-sheikh-frontend:latest \
            --tag ${{ vars.DOCKER_USER }}/agent-sheikh-frontend:${{ github.sha }} \
            --push \
            ./frontend
          
          # Build sandbox
          echo "Building sandbox..."
          docker buildx build \
            --builder cloud-osamabinlikhon-sheikh \
            --platform linux/amd64,linux/arm64 \
            --tag ${{ vars.DOCKER_USER }}/agent-sheikh-sandbox:latest \
            --tag ${{ vars.DOCKER_USER }}/agent-sheikh-sandbox:${{ github.sha }} \
            --push \
            ./sandbox
      
      - name: Testcontainers Cloud build summary
        run: |
          echo "🎉 Testcontainers Cloud build completed successfully!"
          echo "📊 Images built:"
          echo "  - ${{ vars.DOCKER_USER }}/agent-sheikh:latest"
          echo "  - ${{ vars.DOCKER_USER }}/agent-sheikh-backend:latest"
          echo "  - ${{ vars.DOCKER_USER }}/agent-sheikh-frontend:latest"
          echo "  - ${{ vars.DOCKER_USER }}/agent-sheikh-sandbox:latest"
          echo ""
          echo "🚀 To deploy locally:"
          echo "  docker-compose up -d"
          echo ""
          echo "📈 Check Testcontainers Cloud dashboard for performance metrics"

  # Integration Tests with Testcontainers Cloud
  integration-test:
    runs-on: ubuntu-latest
    needs: docker
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    env:
      TC_CLOUD_TOKEN: ${{ secrets.TC_CLOUD_TOKEN }}
      TESTCONTAINERS_CLOUD_ENABLED: true
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Configure Testcontainers Cloud for integration tests
        run: |
          export TC_CLOUD_TOKEN=${{ secrets.TC_CLOUD_TOKEN }}
          export TESTCONTAINERS_CLOUD_ENABLED=true
          
          # Pull and test built images
          echo "📦 Pulling built images..."
          docker pull ${{ vars.DOCKER_USER }}/agent-sheikh:latest
          docker pull ${{ vars.DOCKER_USER }}/agent-sheikh-backend:latest
          docker pull ${{ vars.DOCKER_USER }}/agent-sheikh-frontend:latest
          docker pull ${{ vars.DOCKER_USER }}/agent-sheikh-sandbox:latest
          
          # Test components in Testcontainers Cloud
          echo "🧪 Testing Agent Sheikh components..."
          
          # Test backend health
          echo "Testing backend health..."
          timeout 30s docker run --rm -p 8000:8000 ${{ vars.DOCKER_USER }}/agent-sheikh-backend:latest &
          sleep 10
          curl -f http://localhost:8000/health || echo "Backend health check completed"
          
          echo "✅ Integration tests completed with Testcontainers Cloud!"